// Test stub for the `server-only` guard. Under Next.js the real package
// resolves to a no-op via the "react-server" export condition; Vitest doesn't
// set that condition, so we alias the import to this empty module in tests.
export {};
